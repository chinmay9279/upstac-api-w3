# upstac-api-w3
This repository contains the assignment project for week 3. The project has been uploaded as provided.
The two files LabRequestController and ConsultationController have been updated with the necessary code to implement the functionalities of 
a) "getForTester()" in LabRequestController 
b) "getForConsultations()", "getForDoctor()", "assignForConsultation()" and the "updateConsultation()" methods in ConsultationController
The commits were made separately for each files.
